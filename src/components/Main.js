import React from 'react';
import VoteList from './VoteList';

const Main = () => {
  return(
    <main>
      <div className={'container'}>
        <VoteList />
      </div>
    </main>
  )
};

export default Main;